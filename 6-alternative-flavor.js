module.exports.items = ['item1', 'item2']

const person = {
    name: 'bob',
}

module.exports.singlePerson = person

//used in 3

const kino = (name) => {
    return `haha ${name}`
}

console.log(kino('jino'))
